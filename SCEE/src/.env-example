# --- CONFIGURACIÓN DEL SERVIDOR ---
# Host donde correrá el servidor (ej: 127.0.0.1 o 0.0.0.0)
SERVER_HOST=127.0.0.1
# Puerto para la comunicación TCP
SERVER_PORT=5000

# --- BASE DE DATOS (MariaDB) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=usuario_ejemplo
DB_PASSWORD=password_seguro
DB_NAME=scee_db

# --- ASINCRONISMO Y TAREAS (Celery/Redis) ---
# Formato: redis://:password@host:port/db
REDIS_URL=redis://localhost:6379/0

# --- LOGS Y DEBUG ---
DEBUG=True